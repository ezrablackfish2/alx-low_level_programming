#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints the number of arguments given
 * @argc: the count of the arguments
 * @argv: the arguments to be printed
 * Return: integer
 */
int main(int argc, char *argv[])
{
	argv = argv;
	printf("%d\n", argc - 1);
	return (0);
}
